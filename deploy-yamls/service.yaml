apiVersion: v1
kind: Service
metadata:
  labels:
    app: demo-mode
    environment: Development
  name: demo-mode
  namespace: jobs
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: demo-mode
  sessionAffinity: None
  type: ClusterIP
